interface:
  display_name: "Duforn"
  short_description: "Repo-specific patterns and workflows for duforn"
  default_prompt: "Use the duforn repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true