import { Colors } from '../../vendor/tinyrainbow.mjs';

interface HighlightOptions {
	jsx?: boolean;
	colors?: Colors;
}
declare function highlight(code: string, options?: HighlightOptions): string;

export { highlight };
