// Minimal stub to satisfy Vite build; replace with actual implementation if needed.
export function initPixelatedVideo() {
  // no-op
}

// Auto-init when loaded, if desired
try {
  initPixelatedVideo();
} catch {}
