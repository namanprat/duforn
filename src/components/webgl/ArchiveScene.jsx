import React from "react";

/**
 * Archive route: blank black canvas branch (no ASCII / cube pass).
 */
export default function ArchiveScene() {
  return <color attach="background" args={["#000000"]} />;
}
