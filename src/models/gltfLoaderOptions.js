/**
 * Second argument for every `useGLTF` / `useGLTF.preload` so drei’s loader cache keys match.
 * Add `draco`, `meshopt`, etc. here if the GLBs use those extensions.
 */
export const gltfLoaderOptions = {};
