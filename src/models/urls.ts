// @ts-nocheck
/** Canonical URLs for GLBs — must match `useGLTF` and `useGLTF.preload` exactly.
 *  The website model ships as a plain gltfjsx-loaded GLB (no compression pass).
 *  project-bg is uncompressed; monitor still uses its meshopt-compressed variant.
 */
export const GLTF_URL_PROJECT_BG = "/models/project-bg.glb";
export const GLTF_URL_MONITOR = "/monitor.compressed.glb";
export const GLTF_URL_WEBSITE = "/duforn_website.glb";
export const GLTF_URL_TEST = "/test.glb";
