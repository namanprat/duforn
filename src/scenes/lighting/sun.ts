import * as THREE from "three";

/** Scene sun direction — points toward the light (matches directional shadow setup). */
export const SCENE_SUN_DIR = new THREE.Vector3(0.32, 0.32, 0.89).normalize();
