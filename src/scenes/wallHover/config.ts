// @ts-nocheck
import { DEFAULT_HOME_SCENE_CONTROLS } from "../../store/homeScene";

/** Defaults mirrored in `DEFAULT_HOME_SCENE_CONTROLS.wallHover` (Leva GUI). */
export const WALL_HOVER_DEFAULTS = DEFAULT_HOME_SCENE_CONTROLS.wallHover;
