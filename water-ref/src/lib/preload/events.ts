/**
 * Canonical name for the event fired when the user dismisses the preloader
 * ("Enter"). Dispatched by the preloader UI and listened to by warmup, reveal,
 * and route code. Import this constant instead of hardcoding the string.
 */
export const PRELOADER_DISMISSED_EVENT = "duforn:preloader-dismissed";
