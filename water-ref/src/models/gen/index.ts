// @ts-nocheck
// Side-effect import: registers asset preload tasks at app bootstrap.
// Model components are imported directly from their concrete modules
// (./Website, ./useMonitor, ./useProjectBg), so no re-exports are needed here.
import "./preloads";
